# Each entry is resolved via hydra.utils.get_class() in the training script,
# then composed with ray.rllib.algorithms.callbacks.make_multi_callbacks().
callbacks:
  - _target_: rarl_rllib.callback.CustomMetricsCallback
  - _target_: rarl_rllib.callback.AnnealingCallback
  - _target_: rarl_rllib.callback.TimerCallback
